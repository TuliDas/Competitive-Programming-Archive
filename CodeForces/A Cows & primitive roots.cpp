#include<bits/stdc++.h>
using namespace std;
{
    int prime(int a)
    {
        if(a<=2)
            return a;
        else if(a%2==0)
            return 0;
        else
        {
            for (int j=3;j<sqrt(a);j++)
            {

            }
        }
    }
    int n;
    while(cin>>n)
    {
        int c=0;
        for(int i=1;i<=2000;i++)
        {
            r=prime(i);
        }
    }
}
